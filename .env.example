# python_backend (Gemini Gateway) — server-side only (Vercel / .env.local)
AI_API_URL=https://api.yucelgumus.dev

# Chat + stream (CLIENT_API_KEYS on gateway)
GATEWAY_CLIENT_API_KEY=your_client_api_key

# Upload (dokuman-upsert) + delete (dokuman-sil) — ADMIN_API_KEYS on gateway
GATEWAY_ADMIN_API_KEY=your_admin_api_key

# Local dev fallback names (optional)
# CLIENT_API_KEY=
# ADMIN_API_KEY=
# GEMINI_GATEWAY_URL=